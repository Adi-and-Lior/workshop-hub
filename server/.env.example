# Database Connection
# Replace with your MongoDB Atlas URI or local MongoDB string
MONGO_URI=mongodb+srv://<username>:<password>@cluster.mongodb.net/workshopDB

# Server Configuration
PORT=5000

# Authentication
# Use a random string for JWT signing
JWT_SECRET=your_super_secret_random_string_here